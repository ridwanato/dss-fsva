<!-- BEGIN:user-workflow-rules -->
# Rules for Code Changes and Deployment

1. **Localhost First (Mandatory)**:
   - NEVER execute `git push` directly after making code changes.
   - Always run and test changes locally (`localhost:3000`) first.
   - Wait for explicit user review and confirmation before executing any `git push` to online repository.
<!-- END:user-workflow-rules -->
